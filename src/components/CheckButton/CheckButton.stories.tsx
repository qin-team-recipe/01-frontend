import { CheckButton } from "@/components/CheckButton";

export default {
  component: CheckButton,
};

export const Default = {
  args: {
    name: "チーズ",
    isChecked: false,
    isFirstItem: false,
    isLastItem: false,
  },
};
