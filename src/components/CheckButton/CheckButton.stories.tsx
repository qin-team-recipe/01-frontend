import { CheckButton } from "@/components/CheckButton";

export default {
  component: CheckButton,
};

export const Default = {
  args: {
    name: "チーズ",
    isOwnNotes: true,
    isChecked: false,
    isLastItem: false,
  },
};
