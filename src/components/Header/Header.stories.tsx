import { Header } from "@/components/Header";

export default {
  component: Header,
};

export const Default = {
  args: {
    children: "Header",
    h1: true,
    position: "left",
    href: "test",
  },
};
